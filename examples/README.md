# Examples

These scripts show Silico's library surface without carrying a full experiment.

Run the growing-trajectory example offline:

```bash
python examples/growing_trajectory.py
```

Use the same pattern in an experiment repository: keep your task schedule,
materials, parsing, outputs, and reproducibility records with that experiment,
and import `silico` as the model-facing client.
